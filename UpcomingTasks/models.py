from django.db import models

# # Create your models here.
# class CurrentStatus(models.Model):
#     DY_status = models.IntegerField(null=True, blank=True)  # Allow NULL and empty values from forms/admin
#     FH_status = models.CharField(max_length=255, null=True, blank=True)  # Allow NULL and empty values from forms/admin
#     FC_status = models.IntegerField(null=True, blank=True)   # Allow NULL and empty values from forms/admin
    
#     class Meta:
#         verbose_name = "Current Status"